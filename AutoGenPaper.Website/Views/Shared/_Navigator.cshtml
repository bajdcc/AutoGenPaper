@{ var options = "url:'" + @Url.Action("Navigation", "AGP") + "',animate:true,onClick:function(node){AGPActionCallForAsyncShowContent(node.text, node.attributes.url, false);}"; }
<ul id="menu" class="easyui-tree" data-options="@options">菜单载入中</ul>